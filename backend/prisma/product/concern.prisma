model Concern {
  id          String    @id @default(cuid())
  name        String
  slug        String    @unique
  description String?
  isActive    Boolean   @default(true)
  deletedAt   DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt

  products           ProductConcern[]
  preferenceProfiles UserPreferenceConcern[]

  @@index([name])
  @@index([isActive])
}
